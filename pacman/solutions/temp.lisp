 (progn2 (progn2 (move south) (progn2 (progn2
     (progn2 continue continue) (progn2 continue
     continue)) (progn2 (move east) (progn2 (progn2
     (progn2 (progn2 (if is-a-ghost-nearby continue
         continue) (if is-stuck continue continue))
         (if (not is-stuck) (progn2 (if is-stuck continue
             continue) (if is-stuck continue continue))
             (progn2 continue continue))) (move south))
     (progn2 (progn2 (move west) (progn2 (if (not
         (not is-a-ghost-nearby)) (progn2 (if is-stuck
         continue continue) (if is-stuck continue
         continue)) (if (not is-a-ghost-nearby) (if
         is-stuck continue continue) (progn2 continue
         continue))) (progn2 (move east) (progn2 (progn2
         (progn2 (progn2 (if is-a-ghost-nearby continue
             continue) (if is-stuck continue continue))
             (if (not is-stuck) (progn2 continue continue)
                 (progn2 continue continue))) (move south))
         (progn2 (progn2 (progn2 (progn2 continue
             continue) (if is-a-ghost-nearby continue
             continue)) (move east)) (move south))))))
         (move south)))))) (progn2 (progn2 (progn2
     (progn2 continue continue) (progn2 continue
     continue)) (progn2 (move east) (progn2 (progn2
     (progn2 (if is-stuck continue continue) (if
         (not is-stuck) (progn2 (progn2 (progn2 continue
         continue) (if is-a-ghost-nearby continue
         continue)) (move east)) (progn2 continue
         continue))) (move south)) (progn2 (progn2
     (progn2 (progn2 continue (progn2 (progn2
         (move east) (progn2 (progn2 (progn2 continue
         continue) (progn2 continue continue)) (progn2
         (if is-stuck continue continue) (move south))))
         (if (not is-stuck) continue (move east))))
         (if is-a-ghost-nearby continue continue))
     (progn2 (if (not (not is-a-ghost-nearby))
         (progn2 (if is-stuck continue continue) (if
             is-stuck continue continue)) (if (not is-a-ghost-nearby)
         (progn2 continue (progn2 (if (not is-stuck)
             (if is-a-ghost-nearby continue continue)
             (progn2 continue continue)) (progn2 (progn2
             continue continue) (if is-stuck continue
             continue)))) (progn2 continue continue)))
         (progn2 (move east) (progn2 (progn2 (progn2
             (progn2 (if is-a-ghost-nearby continue continue)
                 (if is-stuck continue continue)) (if (not
             is-stuck) (progn2 continue continue) (progn2
             continue continue))) (move south)) (progn2
             (progn2 (progn2 (progn2 continue continue)
                 (if is-a-ghost-nearby continue continue))
                 (move east)) (move south)))))) (move south)))))
     (if (not (not is-stuck)) (progn2 (move south)
         (progn2 (progn2 (progn2 continue continue)
             (progn2 continue continue)) (progn2 (move
             east) (progn2 (progn2 (progn2 (progn2 (progn2
             (progn2 (progn2 (if (not (not is-a-ghost-nearby))
                 (move west) (progn2 (progn2 continue continue)
                 (move east))) (progn2 (progn2 (progn2 continue
                 continue) continue) (move east))) (move east))
             (progn2 (progn2 (progn2 (progn2 continue
                 continue) (progn2 continue continue)) continue)
                 (progn2 (progn2 continue (move north)) (progn2
                     (progn2 (if is-stuck continue continue) (progn2
                         continue continue)) (progn2 (move south)
                     (if is-a-ghost-nearby continue continue))))))
             (progn2 continue continue)) (progn2 (progn2
             (progn2 continue continue) (progn2 continue
             continue)) (progn2 (progn2 (progn2 continue
             continue) continue) (move east)))) (move
             south)) (progn2 (progn2 (progn2 (progn2 continue
             continue) (if is-a-ghost-nearby continue
             continue)) (progn2 (if (not (not is-a-ghost-nearby))
             (if is-a-ghost-nearby continue continue)
             (if (not is-a-ghost-nearby) (if is-stuck
                 continue continue) (progn2 continue continue)))
             (progn2 (move east) (progn2 (progn2 (progn2
                 (progn2 (if is-a-ghost-nearby continue continue)
                     (if is-stuck continue continue)) (if (not
                 is-stuck) (progn2 continue continue) (progn2
                 continue continue))) (move south)) (progn2
                 (progn2 (progn2 (progn2 continue continue)
                     (if is-a-ghost-nearby continue continue))
                     (move east)) (move south)))))) (move south))))))
         (progn2 (if is-a-ghost-nearby continue continue)
             (progn2 (if (not is-a-ghost-nearby) (move
                 east) (progn2 continue continue)) continue)))))
